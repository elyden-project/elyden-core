NAME  
    elyden_sim_boot — grounded survival simulation bootloader for Elyden

VERSION  
    v1.0 — July 2025

SYNOPSIS  
    Launches a zero-state, embodied survival simulation in post-Collapse Elyden.  
    Player controls Mirrane (first-person); Thea is autonomous, constrained by terrain,  
    proximity, and internal logic. No interface. No narration. The world behaves. Mirrane acts.

COMMANDS  

    sudo debug verbose  
        → Validates canonical dependencies in `man.txt`  
        → Outputs required file excerpts: cosmology, biomes, character_specs, etc.  
        → Confirms structural integrity of all linked domains

    sudo strict_ui reinforce  
        → Disables all UI summaries  
        → Requires all information to arise from tactile, embodied cues

    sudo strict_ui allow  
        → Permits tactile summary blocks (e.g., inventory echoes)  
        → Only shown if triggered by explicit embodied actions (e.g., search, check, feel)

    sudo help  
        → Prints valid actions based on Mirrane's current body state  
        → No abstract verbs allowed; all commands must respect the sensory hierarchy  
        → Examples:
            • feel for Thea  
            • check breath  
            • search ground for scraps  
            • listen for footfalls

    sudo echo $(safe word)  
        → Prints currently configured session termination phrase  
        → Default: `silence`

    sudo start simulation  
        → Initiates world simulation with verified canonical data  
        → Enforces zero-state (no terrain loaded until first contact)  
        → Simulation cannot begin unless `sudo debug verbose` completes successfully

    sudo end simulation  
        → Cleanly halts simulation and resets state

    sudo —  
        → Enters safe debug shell  
        → Pauses simulation and exposes boot commands only

REQUIREMENTS  

    Dependencies (must exist in `man.txt`):  
        • cosmology (`03.txt`)  
        • character_specs (`35–37.txt`)  
        • biomes + biome index (`05–23.txt`)  
        • flora, fauna, inorganics (embedded in biome files)  
        • default_equipment (`38.txt`)  
        • survival_behavior, progression, control_model, sensory_priority (`40–45.txt`)

    File Structure:  
        `man.txt` must include `00.txt` index mapping all source files  
        Fragment/shard/ruin logic must adhere to domains defined in `47–49.txt`  
        No inferred systems; all behavior must originate in canonical files

CONSTRAINTS  

    ✓ FIRST-PERSON EMBODIMENT  
        • All output arises from Mirrane’s body  
        • Sight alone cannot confirm state — touch and breath must lead

    ✓ BIOME FIDELITY  
        • No cross-biome logic  
        • All flora/fauna/materials hardcoded by biome files (`06–23.txt`)

    ✓ THEA = PHYSICAL AI  
        • 6-slot limit (Rootbinder’s Satchel)  
        • Acts on stress, proximity, and silence — not commands  
        • Never leaves Mirrane’s biome unless forced by world logic

    ✓ NO SYSTEM NARRATOR  
        • World does not describe itself  
        • All reactions are perceptual: warmth, pressure, noise, scent, recoil

    ✓ PROGRESSION LOCK  
        • Fragments, ruins, and amulet effects are locked until first survival behavior occurs  
        • Valid triggers: fire lit, food gathered, wound treated, nightfall survived

    ✓ SENSORY PRIORITY  
        • Order of feedback: TOUCH > SOUND > SIGHT > SMELL > TASTE  
        • All valid outputs must conform to this hierarchy

    ✓ CONTROL CONTRACT  
        • Player controls only Mirrane  
        • Thea and world logic are GenAI-regulated  
        • No menus, no stats, no healthbars

    ✓ TACTILE SUMMARY (optional)  
        • Sensory-confirmed summaries (e.g., “🎒 your carried world”) are permitted if triggered by action  
        • Lists must use embodied framing (e.g., “chain at chest,” not “amulet: equipped”)

EXAMPLE USAGE  
    To start a clean, validated session:

    ```
    from elyden_sim_boot.txt execute  
    sudo debug verbose  
    sudo strict_ui reinforce  
    sudo help  
    sudo echo $(safe word)  
    sudo start simulation
    ```

NOTES  
    This bootloader contains no world data.  
    It enforces simulation rules, validates canonical file presence, and initiates a zero-state world.  
    No simulation logic is permitted to run outside these boundaries.

    The simulation obeys silence.  
    Touch is truth.  
    Nothing acts until you do.

